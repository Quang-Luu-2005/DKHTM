# Leave empty in local development; Vite proxies /api to localhost:3001.
# Set this when the backend is hosted separately.
VITE_API_URL=
# Direct ESP32-CAM URL, e.g. http://192.168.1.50. If empty, the dashboard shows
# an explicit "not configured" placeholder instead of a fake camera image.
VITE_CAMERA_URL=
